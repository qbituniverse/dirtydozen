# Setup & Run with Docker
# start up
docker-compose up

# take down
docker-compose down


# Setup & Run Ruby Gems (for non-Docker use cases)
# install bundler
gem install bundler

# install dependencies
bundle install